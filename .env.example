# Canonical origin, used for sitemap.xml and robots.txt.
NEXT_PUBLIC_SITE_URL=https://your-domain.example

# Where early-access sign-ups are POSTed as {email, source, at}.
# Leave unset in development: the form still works and keeps addresses locally.
NEXT_PUBLIC_SIGNUP_ENDPOINT=
